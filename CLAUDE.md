# 골라! — 저장소 안내

> 상위 지침: [`docs/프로젝트-지침.md`](docs/프로젝트-지침.md) (중독성 있는 간단한 게임).
> 이 게임의 요청서: [`docs/3rd-game-개발-요청서.md`](docs/3rd-game-개발-요청서.md).
> 충돌하면 지침이 우선이고, 어긴 곳은 `README.md` 7장에 이유를 적는다.

- **규칙:** 위에서 떨어지는 도형이 지금 조건 색이면 탭한다. 아니면 그냥 보낸다.
- **스킬:** 억제(누르지 말 것을 참기) + 반응.

## 파일 구조

```
index.html                 게임 전체 (단일 파일, 빌드 없음, 외부 의존 0)
CLAUDE.md                  이 문서 — 구조·상태 머신·훅·컨벤션
README.md                  규칙·점수식·DIFFS 값과 이유·봇 결과·지침을 어긴 곳
test/autoplay.mjs          Playwright 자동 플레이 봇 3종 + 재현성 검증
docs/                      상위 지침 + 이 게임의 개발 요청서 (원본)
package.json               테스트 실행용(playwright devDependency)만
.github/workflows/pages.yml  main 푸시 → GitHub Pages 배포
```

`index.html` 안의 순서:

| 구역 | 내용 |
|---|---|
| 상수 | 논리 좌표, 구역 경계, 전환/정지 시간 |
| `DIFFS` | 난이도 표 **하나**. 튜닝은 여기만 고친다 (`speed` 가 구역 = 시간 창을 전부 정한다) |
| `COLORS` | 색 4종 + 색맹 보조 기호 |
| `mulberry32` | 게임 로직의 **유일한** 난수원 |
| 시뮬레이션 | `createSim` / `simStep` / `simTap` / `doTap` / `resolveFloor` / `afterResolve` — 순수. DOM·오디오를 모른다 |
| `Store` | 저장 어댑터 `window.storage` → `localStorage` → 메모리 |
| `LB` | 리더보드 어댑터: 아티팩트 `db` → Supabase REST → 꺼짐 |
| 오디오 | Web Audio 합성. 첫 제스처 후 `audioInit()` |
| 음악 | `MUSIC` 3곡 + `musicTick()` 선행 예약 스케줄러. **합성만, 파일 없음** |
| 연출 | 파편·텍스트·흔들림·플래시. **여기서만 `Math.random` 허용** |
| 앱 | 화면 전환, 입력, 메인 루프, 그리기 |
| `judgeReplay` | 재현 검증 (렌더 없이 다시 돌린다) |
| `window.__judge` | 자동 플레이 훅 |

## 상태 머신

```
        탭                    목숨 0            탭(0.6초 락 후)
title ─────► play ──────────────────────► over ─────────────┐
              ▲                                              │
              └──────────────────────────────────────────────┘
```

- `SCREEN` (`title` / `game` / `over`) 은 앱 레벨, `G.phase` (`play` / `over`) 는 시뮬레이션 레벨.
- **`switching` 단계는 없다.** 기준 전환에 정지가 없기 때문이다(아래 참고).
- 목숨은 3개(`LIVES`, 요청서 그대로). 한 판 약 51초 — README 4장.
- **WRONG 정지(250ms)** 와 **마지막 목숨을 MISS 로 잃었을 때(380ms)** 는 별도 상태가 아니라
  `G.freeze` 카운터다. 정지 중에는 세계가 멈추고 입력은 버려진다. 지금 게임에서 가장 긴 공백이다.

## 재현성 (원칙 1)

**고정 타임스텝.** 1틱 = 1프레임 = 1000/60 ms. `requestAnimationFrame` 은 누산기로
틱을 몰 뿐이고(프레임당 최대 3틱 = dt 클램프 [0,3]), 시뮬레이션은 주사율과 무관하게
항상 같은 틱 열을 밟는다. 렌더만 남은 시간으로 보간해서 고주사율에서도 부드럽다.

- 시드가 정하는 것: 도형 색 순열, 기준 색 순열.
- 시드가 정하지 **않는** 것: 결과.
- 입력 로그는 `[{t: 게임시간ms}]`. `t / (1000/60)` 이 정확히 틱 번호다.
- `window.__judge.replay(seed, inputs)` 가 같은 점수를 내지 못하면 원칙 1 위반.
  게임 오버 화면의 `시드 · 재현` 버튼이 이 검증을 돌리고 콘솔에 결과를 찍은 뒤 그 판을 재생한다.

## 자동 플레이 훅

```js
window.__judge = {
  reset(seed),          // 새 판 + MANUAL 모드(rAF 스텝 정지). 시간은 step() 이 민다
  step(dtFrames),       // 정수 틱만큼 진행. 렌더 없이도 동작. state() 를 돌려준다
  tap(),                // 입력 큐에 1회. 다음 step() 의 맨 앞에서 처리된다
  state(),              // { score, lives, level, active, condition, over, ...추가필드 }
  replay(seed, inputs), // 별도 시뮬레이션으로 재현. 현재 판을 건드리지 않는다
  lastResult(), manual(on), DIFFS,
};
```

`state().active` 는 요청서의 `{color, y}` 에 `id` 와 `mult` 를 더한다(봇이 "같은 도형"을
추적해야 해서). `timeMs` / `tick` / `processed` / `stats` / `phase` 도 봇 편의용 추가 필드다.

## 컨벤션

- 단일 `index.html`. 빌드 없음. 외부 라이브러리·에셋·폰트 없음.
- Canvas 2D + Web Audio 합성. 오디오는 첫 사용자 제스처 이후. **음악도 합성이다** —
  `MUSIC` 3곡(맥박/물결/톱니)은 마이너 펜타토닉 + Am–F–C–G 순환이라 어디서 끊어도
  어긋나지 않는다. `musicTick()` 이 `AudioContext.currentTime` 기준으로 0.25초치를
  미리 예약하므로 rAF 가 흔들려도 박자는 흔들리지 않는다.
  컨트롤은 바닥선 아래 한 줄(`MBTN` / `drawMusicBar` / `hitMusicBar`) — 레인 밖이라
  눌러도 게임 탭으로 새지 않는다. 스피커는 효과음까지 포함한 전체 음소거(`muted`).
- 게임 로직에서 `Math.random` 금지. 연출(파편·흔들림)에만 허용.
- 난이도는 `DIFFS` 한 표. 다른 곳에 숫자를 흘리지 않는다.
- UI 텍스트는 한국어. 시스템 폰트.
- `prefers-reduced-motion: reduce` 면 흔들림·확대·파편을 끄고 색·텍스트·소리만 남긴다.
- 입력: 터치·마우스·스페이스·엔터 모두 "탭". `touchstart` 는 `preventDefault`.
- 저장은 어댑터로만.
- **시스템은 하나만** (원칙 9). 콤보·덱·아이템·스테이지 제안은 코드가 아니라 README 에 적는다.

## 리더보드 기록은 스스로 검산된다

기록에 `seed` 와 입력 로그가 함께 들어가므로 순위표를 여는 쪽에서
`judgeReplay(seed, inputs)` 로 점수를 확인한다(`verifyRow`). 점수만 조작하면 `✕` 로 드러난다.
**HUD 의 `세계 1위` 는 검산을 통과한 기록만 쓴다** — 위조된 점수가 목표가 되면 안 된다.
튜닝(`TUNED`)이 걸린 판은 등록도 검산도 하지 않는다(기본 난이도와 비교 불가).
남이 넣은 이름은 신뢰할 수 없는 입력이다 — **언제나 `textContent`**, `innerHTML` 금지.

백엔드는 어댑터다: `claude.use('db')`(아티팩트, 설정 불필요) → Supabase REST
(`window.__judgeLB`) → 꺼짐. **`db` 를 선언한 아티팩트는 조직 내부 전용이 되어
공개 공유가 막힌다** — 공개로 돌리려면 Supabase 쪽. 문서는 이름당 하나라 무한히 늘지 않는다.

## 기준은 큐다 — `afterResolve` 를 판정 뒤에 부를 것

`sim.cond`(지금) 과 `sim.nextCond`(다음)는 언제나 유효하고, HUD 가 **둘 다** 보여준다.
그래서 전환에 정지도 배너도 필요 없고, 기준을 **매 도형마다**(`switchEvery: 1`) 바꿀 수 있다.

`afterResolve(sim)` 이 큐를 한 칸 민다. **반드시 판정이 끝난 뒤에 부른다** —
먼저 부르면 방금 그 도형을 새 기준으로 재게 된다. `doTap` 과 `resolveFloor` 의
마지막 줄이 그 자리다. 큐는 화면에 도형이 없는 순간에만 밀리므로,
요청서가 걱정한 "판정 조건이 애매해지는 순간"은 구조적으로 생길 수 없다.

## 코어 루프의 불변식 둘

**1. 화면에는 언제나 도형이 하나뿐이고, 언제나 맨 위에서 시작한다.**
그래서 `y` 는 위치가 아니라 **플레이어가 망설인 시간**이고, 구역 배율은 그 시간을 잰다.
이게 깨지면 배율이 "내 반응"이 아니라 "화면이 얼마나 비었나"를 재게 되고
위험-보상 축이 즉시 뒤집힌다. 2차 플레이테스트에서 실제로 그랬다(README 9장).

**2. 생성은 고정 박자다 — `period` 프레임마다 하나. 플레이와 무관하다.**
메트로놈처럼 일정해야 플레이어가 "같은 색인가"만 판단하면 된다(6차, 기획자 지시).

두 불변식은 **`period` ≥ 낙하 프레임** 일 때만 동시에 성립한다.
주기가 낙하보다 짧으면 도형이 둘 겹치거나(1번 위반) 생성이 건너뛰어져 박자가 끊긴다(2번 위반).
코드가 시작할 때 `checkPeriod()` 로 검사하고 `console.warn` 한다. **이 부등식을 깨지 마라.**

결과: **생존은 정확도만이, 점수는 배율만이 정한다.** 세 react 봇(200/300/400ms)의
생존 시간이 정확히 같은 것이 그 증거다(README 4장). 축이 깨끗하게 직교한다.
빠른 플레이의 보상은 템포가 아니라 **점수 + 다음 기준을 읽을 여유**다.

도형을 둘 이상 두거나, 생성을 다시 "처리 즉시"로 되돌리거나, 도형이 맨 위가 아닌
곳에서 활성화되게 하는 변경은 **먼저 README 9장에 근거를 적고** 하라.

## 점수는 칸이 아니라 연속이다 — 눈금은 사람의 인식 시간 위에

`HIT 점수 = round(30 · (1−p)^1.4)`, 최소 1점. `p` 는 판정 구간
(`ACTIVE_MIN`~`FLOOR`, 548px) 안의 진행도. 눈금(20/20/20/40 로 나눈 띠):

| 눈금 | 레벨 0 | 뜻 |
|---|---|---|
| 30점 | 0ms | 알기 전에 지른다 — 반응으로 닿지 않는다. 숙련의 천장 |
| 22점 | 153ms | |
| **15점** | **~306ms** | **아는 순간 바로 친다 — 인식 시간 경계. 이 게임의 고민이 사는 자리** |
| 8점 | ~458ms | 알고 나서 친다 |
| 1점 | ~761ms | 망설였다. 그 뒤는 바닥 = MISS |

**15점 눈금(306 → 262ms)을 인식 시간에서 떼어놓지 마라.** 이 한 숫자가 재미를 정한다.

**왜 칸이 아니라 연속인가**(7차): 칸 경계는 점수 절벽인데 사람들이 몰리는 지점이
하필 그 경계라, 반응 떨림(±45ms)이 같은 실력을 30점과 20점으로 갈랐다 — 원칙 1 위반이다.
연속으로 두면 한 도형 단위 판별력이 17% 오른다. 순위 자체는 안 바뀐다(둘 다 Cohen d 0.80) —
바뀌는 것은 **매 순간의 피드백이 정직한가**다. README 8.5장에 측정이 있다.

**지수 1.4 를 직선으로 펴지 마라.** 아래 40% 의 평평한 구간이 "이미 늦었으면 쉬어가는"
자리이고(피로도), 직선이면 safe/perfect 가 40% 라 완료 기준 4에 걸린다.
`?score=linear|tier` 로 비교만 할 수 있게 남겨 뒀다.
띠 색·크기·소리·화면 튐은 여전히 `MULT` 표에서 가져온다 — 숫자는 정밀하게, 느낌은 또렷하게.

화면 양옆 여백(레인은 58~302)은 각각 쓰인다: **왼쪽**은 점수 사다리 눈금,
**오른쪽**은 `drawLog()` — 방금 얻은 점수가 맨 위에 얹히고 과거가 한 칸씩 내려간다.
둘 다 레인 밖이라 판정 화면과 겹치지 않는다.
`speed` 는 그래서 전 레벨 12.0~14.0 으로 거의 눕혀 두었다.
배율 색·크기·소리·화면 튐은 `MULT` 표 한 곳에 있다.
**난이도를 speed 로 올리지 마라.** `lure`(0.40 → 0.68)와 `period`(967 → 750ms)로 올린다.
주기가 줄면 **다음 기준을 미리 읽을 틈**이 줄어든다 — 지각이 아니라 준비를 압박한다.

## 난이도를 손으로 재보기

```
index.html?speed=12.6&lure=0.5&switch=5&period=55&score=linear
```

준 값으로 모든 레벨을 평평하게 만든다(`TUNED`). 화면 아래에 「튜닝됨」이 뜨고,
그 상태의 기록은 기본 난이도와 비교할 수 없다.

## 튜닝할 때 반드시 지킬 것

`perfect` 봇은 반응 0ms 다. **perfect 봇 기준으로 배율 도달 가능성을 맞추면 안 된다** —
1차 튜닝이 정확히 그 실수였고, 봇 기준은 전부 통과하는데 사람은 ×1 만 잡았다.
배율·난이도를 건드릴 때는 `react(ms)` 와 속도-정확도 모델(README 4장)로 확인한다.

## 테스트

```bash
npm test                                  # 기본 8 시드, 상한 120초
node test/autoplay.mjs --seeds 20 --max 90
```

봇 수치는 **밸런스 확인용이고 재미의 증거가 아니다.** 재미는 직접 10판 해보고 판단한다.